[defaults]
# Hexlet запускает playbook как code/playbook.yml из /project.
# Этот путь нужен, чтобы находились роли `docker`/`deploy_compose`.
roles_path = code/ansible/roles:ansible/roles
retry_files_enabled = false
stdout_callback = yaml

[privilege_escalation]
become = true
become_method = sudo
